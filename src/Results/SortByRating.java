package Results;

import Database.Item;

import java.util.Comparator;

/*public class SortByRating implements Comparator<Item> {
    @Override
    public int compare(Item item1, Item item2) {
        int comparison = 0;
        double difference = item2.getRating() - item1.getRating();
        if (difference > 0) {
            comparison = (int) Math.ceil(difference);
        } else {
            comparison = (int) Math.floor(difference);
        }
        return comparison;
    }
}*/
